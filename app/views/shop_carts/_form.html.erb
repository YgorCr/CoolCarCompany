<%= form_for(@shop_cart) do |f| %>
  <% if @shop_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shop_cart.errors.count, "error") %> prohibited this shop_cart from being saved:</h2>

      <ul>
      <% @shop_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :total %><br>
    <%= f.number_field :total %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
